#include <stdio.h> 

int main(){

   float n1, n2, n3, n4, peso;

   printf("Digite a 1° nota: ");
   scanf(" %f", &n1);

   printf("Digite a 2° nota: ");
   scanf(" %f", &n2);

   printf("Digite a 3° nota: ");
   scanf(" %f", &n3);

   printf("Digite a 4° nota: ");
   scanf(" %f", &n4);

   peso = (((n1*2) + (n2*3) + (n3*3) + (n4*2))/10);

   printf("A média ponderada é igual á: %.2f\n", peso);

   return 0;

}
